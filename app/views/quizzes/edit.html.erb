<% if session[:type] == "teacher" %>
	
	<h1>Editing quiz</h1>
	<%= @quiz_var.name %>

	<%= render 'form' %>

	<%= link_to 'Show', @quiz %> |
	<%= link_to 'Back', quizzes_path %>
	<% session[:quiz_id] = @quiz_var.id %>
	<%= link_to("New Question", new_question_path(id: @quiz_var.id) ) %>

<% end %>
