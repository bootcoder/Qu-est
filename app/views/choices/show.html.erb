<p id="notice"><%= notice %></p>

<p>
  <strong>Body:</strong>
  <%= @choice.body %>
</p>

<p>
  <strong>Correct:</strong>
  <%= @choice.correct %>
</p>

<% session[:choice_id] = @choice.id %>

<%= link_to 'Edit', edit_choice_path(@choice) %> |
<%= link_to 'Back', question_path(session[:question_id]) %>
